"""Ex.1 เขียนโปรแกรมเพื่อรับจำนวนจริงบรรทัดละจำนวน หยุดรับจำนวนเมื่อข้อมูลเข้าเป็นสตริงว่าง แล้วหาว่าในจำนวนที่รับเข้ามาทั้งหมด มีจำนวนที่น้อยที่สุดอยู่กี่จำนวน แสดงจำนวนที่นับได้"""

ls = []

while True:
    x = input()

    if x == '':
        break

    ls.append(float(x))

ls.sort()

print(ls.count(ls[0]))