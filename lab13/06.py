# เขียนโปรแกรมรับสตริงบรรทัดละจำนวน จนกว่าข้อมูลเข้าจะเป็นสตริงว่าง แสดงผลลัพธ์เป็นสตริงใหม่ที่นำข้อมูลเข้าทั้งหมดมาต่อกัน

ls = []

while True:
    x = input()

    if x == '':
        break

    ls.append(x)

print(''.join(ls))